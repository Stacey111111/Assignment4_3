# Assignment 4 - 中文使用指南

## 📚 目录

1. [项目概览](#项目概览)
2. [文件说明](#文件说明)
3. [快速开始](#快速开始)
4. [任务详解](#任务详解)
5. [参数校准](#参数校准)
6. [常见问题](#常见问题)

---

## 项目概览

### 完成的任务

本作业实现了完整的视觉抓取系统：

```
Part C-a (20分): 视觉伺服 - 对准瓶子
Part C-b (30分): 抓取瓶子 - 靠近、抓住、举起
Part C-c (20分): 抓取放置 - 返回基地、放下
```

### 系统架构

```
YOLO 发布者 (Jetson)
    ↓ 发布检测结果
任务节点
    ↓ 控制命令
机器人硬件
```

---

## 文件说明

| 文件 | 用途 |
|------|------|
| **yolo_publisher_COMPLETED.py** | YOLO 目标检测发布者 |
| **yolo_subscriber_COMPLETED.py** | YOLO 订阅者（测试用） |
| **task_1a_visual_servoing.py** | Part C-a：视觉伺服 |
| **task_1b_pick_bottle.py** | Part C-b：抓取瓶子 |
| **task_2_pick_and_place.py** | Part C-c：抓取并放置 |

---

## 快速开始

### 第一步：启动 YOLO 发布者

```bash
# 终端 1（Jetson 上运行）
cd ~/turtlebot3_ws/src/assignment_4
python3 yolo_publisher_COMPLETED.py
```

### 第二步：运行任务

```bash
# 终端 2（Jetson 或 Remote-PC）

# 任务 1a - 视觉伺服
python3 task_1a_visual_servoing.py

# 或者 任务 1b - 抓取瓶子
python3 task_1b_pick_bottle.py

# 或者 任务 2 - 抓取并放置
python3 task_2_pick_and_place.py
```

---

## 任务详解

### Part C-a: 视觉伺服（20分）

**功能：** 机器人转动，保持瓶子在相机视野中心

**工作流程：**
```
1. YOLO 检测到瓶子
2. 计算误差：瓶子中心 - 图像中心
3. 比例控制器：角速度 = -Kp × 误差
4. 机器人左右转动，直到瓶子居中
```

**运行方法：**
```bash
# 终端 1
python3 yolo_publisher_COMPLETED.py

# 终端 2
python3 task_1a_visual_servoing.py
```

**预期行为：**
1. 机器人慢慢旋转寻找瓶子
2. 发现瓶子后，转动对准
3. 瓶子居中时停止
4. 日志显示：`✓ CENTERED | Bottle at x=640, error=0px`

**测试方法：**
1. 把瓶子放在机器人前方，但偏左或偏右
2. 机器人应该转动对准瓶子
3. 慢慢左右移动瓶子
4. 机器人应该跟随，保持瓶子居中

---

### Part C-b: 抓取瓶子（30分）

**功能：** 对准 → 靠近 → 抓住 → 举起

**状态机：**
```
SEARCHING（搜索）
    ↓
CENTERING（对准）
    ↓
APPROACHING（靠近）
    ↓
POSITIONING_ARM（手臂定位）
    ↓
GRASPING（抓取）
    ↓
LIFTING（举起）
    ↓
DONE（完成）
```

**状态说明：**

| 状态 | 动作 | 切换条件 |
|------|------|----------|
| SEARCHING | 旋转寻找 | 检测到瓶子 |
| CENTERING | 对准瓶子 | 居中（±50像素） |
| APPROACHING | 前进 | 边界框面积 ≥ 80,000 |
| POSITIONING_ARM | 移动手臂 | 3秒后 |
| GRASPING | 闭合夹爪 | 2秒后 |
| LIFTING | 抬起手臂 | 3秒后 |
| DONE | 停止 | - |

**运行方法：**
```bash
# 终端 1
python3 yolo_publisher_COMPLETED.py

# 终端 2
python3 task_1b_pick_bottle.py
```

**预期行为：**
1. **SEARCHING**: 旋转寻找瓶子
2. **CENTERING**: 对准瓶子
3. **APPROACHING**: 前进靠近
4. **POSITIONING_ARM**: 手臂移动到预抓取位置
5. **GRASPING**: 夹爪闭合
6. **LIFTING**: 举起瓶子
7. **DONE**: 完成

---

### Part C-c: 抓取并放置（20分）

**功能：** 抓取瓶子 → 返回基地 → 放下瓶子

**扩展的状态机：**
```
[前 6 个状态同 Part C-b]
    ↓
RETURNING_HOME（返回基地）
    ↓
LOWERING_ARM（降低手臂）
    ↓
RELEASING（释放瓶子）
    ↓
RETURN_COMPLETE（完成）
```

**新增状态：**

| 状态 | 动作 | 目的 |
|------|------|------|
| RETURNING_HOME | 导航回起点 | 返回基地 |
| LOWERING_ARM | 降低手臂 | 准备放置 |
| RELEASING | 打开夹爪 | 释放瓶子 |
| RETURN_COMPLETE | 停止 | 任务完成 |

**运行方法：**
```bash
# 终端 1
python3 yolo_publisher_COMPLETED.py

# 终端 2
python3 task_2_pick_and_place.py
```

**启动流程：**
```
⚠️  Move robot to HOME BASE and press Enter...
[你手动把机器人放到想要的基地位置]
[按 Enter]
✓ Recording home position...
✓ Home position recorded: (0.000, 0.000), yaw=0.000
```

**预期行为：**
1. 状态 1-6：同 Part C-b（抓取瓶子）
2. **RETURNING_HOME**: 导航回起始位置
3. **LOWERING_ARM**: 手臂降低到地面
4. **RELEASING**: 夹爪打开，释放瓶子
5. **RETURN_COMPLETE**: 任务完成

---

## 参数校准

### ⚠️ 最重要：目标边界框面积

```python
self.target_bbox_area = 80000  # 像素²
```

**必须校准这个值！**

**校准步骤：**

1. **运行订阅者查看检测数据：**
```bash
python3 yolo_subscriber_COMPLETED.py
```

2. **手动驾驶机器人到合适的抓取距离：**
```bash
ros2 run turtlebot3_teleop teleop_keyboard
```

3. **记录边界框大小：**
```
输出示例：
[0] bottle (0.85) | Center: (640.0, 400.0), Size: 250.0x320.0
                                                      ↓      ↓
                                                    宽度    高度
面积 = 宽 × 高 = 250 × 320 = 80,000
```

4. **更新代码中的 `target_bbox_area`**

**校准示例：**
```
太远（面积=30,000）：  瓶子太小
完美（面积=80,000）：  ✓ 合适的抓取距离
太近（面积=150,000）： 可能碰撞
```

### 可选参数调整

#### 1. 对准容差

```python
self.center_tolerance_x = 50  # 像素
```

- **更小（20-30）**：更精确，但更慢
- **更大（70-100）**：更快，但不太精确

#### 2. 控制增益

```python
# 旋转
self.kp_angular = 0.002  # 比例增益
self.angular_speed_max = 0.3  # rad/s

# 靠近
self.kp_linear = 0.000005  # 比例增益
self.linear_speed = 0.1  # m/s
```

**调参建议：**
- 如果机器人震荡：减小 `kp_angular`
- 如果机器人太慢：增大增益
- 如果机器人太快/不稳定：减小最大速度

#### 3. 手臂位置

```python
# 预抓取位置
point.positions = [0.0, -0.5, 0.8, 0.3]
#                  │     │     │    │
#                  │     │     │    └─ 关节4（手腕）
#                  │     │     └─ 关节3（肘部）
#                  │     └─ 关节2（肩部）
#                  └─ 关节1（基座旋转）
```

**如何找到合适的位置：**
1. 使用 `rqt_joint_trajectory_controller` 手动移动手臂
2. 找到好的预抓取位置
3. 记录关节角度
4. 在代码中更新

---

## 常见问题

### 问题 1：机器人检测不到瓶子

**症状：**
```
Searching for bottle...
Searching for bottle...
（一直找不到）
```

**解决方案：**
1. 检查 YOLO 发布者是否在运行
2. 检查瓶子置信度 > 0.5
3. 确认光照条件良好
4. 尝试不同的瓶子（更明显）

**调试：**
```bash
# 查看 YOLO 检测结果
python3 yolo_subscriber_COMPLETED.py

# 应该看到：
[0] bottle (0.85) | Center: (...), Size: ...
```

---

### 问题 2：机器人停得太远或太近

**症状：**
- 停得太远：夹爪够不到瓶子
- 停得太近：撞到瓶子

**解决方案：**
校准 `target_bbox_area`：

```python
# 如果停得太远：
self.target_bbox_area = 80000  # 增大这个值
                       ↓
                     100000

# 如果停得太近：
self.target_bbox_area = 80000  # 减小这个值
                       ↓
                      60000
```

---

### 问题 3：手臂够不到瓶子

**症状：**
- 夹爪错过瓶子
- 夹爪太高/太低

**解决方案：**

1. **调整靠近距离**（修改 `target_bbox_area`）

2. **调整手臂位置：**
```python
# 让手臂伸得更低：
point.positions = [0.0, -0.5, 0.8, 0.3]
                        ↓
                       -0.7  # 更向下

# 让手臂伸得更高：
point.positions = [0.0, -0.5, 0.8, 0.3]
                        ↓
                       -0.3  # 更向上
```

---

### 问题 4：夹爪不能正常闭合

**症状：**
- 夹爪打开而不是闭合
- 夹爪不动

**解决方案：**

1. **检查夹爪动作服务器：**
```bash
ros2 action list
# 应该看到：/tool_control
```

2. **手动测试夹爪：**
```bash
# 闭合
ros2 action send_goal /tool_control open_manipulator_msgs/action/ToolControl "{planning_group: gripper, value: -0.01}"

# 打开
ros2 action send_goal /tool_control open_manipulator_msgs/action/ToolControl "{planning_group: gripper, value: 0.01}"
```

3. **调整夹爪数值：**
```python
# 闭合夹爪
goal.value = -0.01  # 尝试：-0.015, -0.02

# 打开夹爪
goal.value = 0.01   # 尝试：0.015, 0.02
```

---

### 问题 5：机器人不返回基地（任务 2）

**症状：**
- 机器人随机游荡
- RETURNING_HOME 状态时不动

**解决方案：**

1. **检查里程计：**
```bash
ros2 topic echo /odom
# 应该看到位置更新
```

2. **检查基地位置是否记录：**
```
✓ Home position recorded: (0.000, 0.000), yaw=0.000
```

3. **增加位置容差：**
```python
self.position_tolerance = 0.1  # 尝试 0.2 或 0.3
```

---

### 问题 6：机器人震荡（左右摇摆）

**症状：**
- 对准时左右晃动
- 靠近时加速/减速反复

**解决方案：**

**减小比例增益：**
```python
# 旋转震荡
self.kp_angular = 0.002  # 尝试 0.001
                 ↓
                0.001

# 靠近震荡
self.kp_linear = 0.000005  # 尝试 0.000003
                 ↓
                0.000003
```

**限制速度：**
```python
self.angular_speed_max = 0.3  # 减小到 0.2
self.linear_speed = 0.1  # 减小到 0.05
```

---

## 视频演示清单

### Part C-a：视觉伺服（20分）

**需要展示：**
1. ✓ 机器人启动，瓶子在视野中但偏离中心
2. ✓ 机器人转动对准瓶子
3. ✓ 日志显示 "CENTERED" 消息
4. ✓ 慢慢左右移动瓶子
5. ✓ 机器人跟随，保持居中锁定

**讲解要点：**
- "机器人使用比例控制"
- "误差是从图像中心计算的像素距离"
- "角速度与误差成比例"

---

### Part C-b：抓取瓶子（30分）

**需要展示：**
1. ✓ 把瓶子放在地上，稍微偏离中心
2. ✓ 启动节点，显示日志中的状态转换
3. ✓ 机器人搜索（旋转）
4. ✓ 机器人对准瓶子
5. ✓ 机器人靠近瓶子
6. ✓ 手臂移动到预抓取位置
7. ✓ 夹爪闭合
8. ✓ 手臂举起瓶子

**讲解要点：**
- "状态机有 7 个状态"
- "机器人使用边界框面积判断距离"
- "面积阈值校准为 [你的值]"
- "夹爪使用动作客户端控制"

---

### Part C-c：抓取并放置（20分）

**需要展示：**
1. ✓ 把机器人放在基地位置
2. ✓ 按 Enter 记录基地
3. ✓ 把瓶子放在附近
4. ✓ 机器人抓取瓶子（状态 1-6）
5. ✓ 机器人返回基地
6. ✓ 机器人放下瓶子
7. ✓ 日志显示 "PICK AND PLACE COMPLETE"

**讲解要点：**
- "使用里程计记录基地位置"
- "使用 atan2 计算回基地的航向"
- "位置容差是 [你的值] 米"
- "夹爪用正值打开"

---

## 快速命令参考

```bash
# 查看 YOLO 检测
python3 yolo_subscriber_COMPLETED.py

# 测试里程计
ros2 topic echo /odom

# 查看可用话题
ros2 topic list

# 手动控制机器人
ros2 run turtlebot3_teleop teleop_keyboard

# 测试夹爪
ros2 action send_goal /tool_control open_manipulator_msgs/action/ToolControl "{planning_group: gripper, value: -0.01}"
```

---

## 成功提示

1. ✅ **一定要校准 `target_bbox_area`** - 最重要！
2. ✅ **逐步测试** - 先分别运行每个任务
3. ✅ **查看日志** - 状态转换显示进度
4. ✅ **从慢速度开始** - 稳定后再提速
5. ✅ **良好光照** - YOLO 在好光线下效果更好

---

**祝您演示成功！** 🤖🍾
